"use client";

import BugTrackerPage from "@/screens/bug-tracker/BugTrackerPage";

// system_admin: every report, with triage (drag / status changes).
export default function Page() {
  return <BugTrackerPage />;
}
